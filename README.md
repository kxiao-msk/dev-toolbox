# dev-toolbox

### Notes
```
the default path is `~/Workspace/Toolbox/dev-toolbox`, which affects the vcolumes config for Kafka.
If you checkout to different localtion, please update the `volumes` config in `docker-compose/mq/kafka-all-in-one-sasl.yaml` file.
```
